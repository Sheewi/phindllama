# app_engine.yaml - Simplified Google App Engine configuration
runtime: python39
instance_class: F2

entrypoint: gunicorn -b :$PORT main:app

handlers:
- url: /.*
  script: auto
  secure: always

env_variables:
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
